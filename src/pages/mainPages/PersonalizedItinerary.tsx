import {FC, JSX} from 'react';

const PersonalizedItinerary: ({}: {}) => JSX.Element = ({}) => {
    return (
        <div>
            
        </div>
    );
};

export default PersonalizedItinerary;
